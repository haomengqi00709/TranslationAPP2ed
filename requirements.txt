# Lightweight requirements for Railway deployment
# Railway only acts as API proxy - no ML computation here!
# ML work happens on RunPod (uses requirements.txt)

# FastAPI and web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# PowerPoint processing (lightweight - only for file handling)
python-pptx>=0.6.21

# RunPod API client (to call RunPod serverless endpoint)
runpod>=1.0.0

# HTTP client
requests>=2.31.0

# Optional: Cloud translator APIs (lightweight clients)
openai>=1.0.0
anthropic>=0.21.0
