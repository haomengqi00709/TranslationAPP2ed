[phases.setup]
nixPkgs = ["...", "libreoffice", "poppler_utils"]
aptPkgs = ["chromium", "chromium-driver", "fonts-liberation", "fonts-noto-cjk"]

[phases.build]
cmds = ["...", "playwright install chromium"]

[start]
cmd = "uvicorn api:app --host 0.0.0.0 --port $PORT"
