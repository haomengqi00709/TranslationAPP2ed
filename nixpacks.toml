# Nixpacks configuration for Railway deployment
# Tells Railway to use lightweight requirements file

providers = ["python"]

[phases.install]
cmds = ["pip install -r requirements-railway.txt"]

[start]
cmd = "uvicorn api:app --host 0.0.0.0 --port $PORT"
